import React from "react";
import Nav from "../Nav/Nav";

const Header = () => {
  return (
    <header>
      <Nav />
      <div className="logo">LOGO</div>
    </header>
  );
};

export default Header;
